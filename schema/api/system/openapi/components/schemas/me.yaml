MultiFactor:
  type: object
  required:
    - factorId
    - phoneNumber
  properties:
    factorId:
      type: string
    phoneNumber:
      type: string

Me:
  type: object
  required:
    - emailVerified
    - membership
  properties:
    emailVerified:
      type: boolean
      default: false
    multiFactor:
      $ref: "#/components/schemas/MultiFactor"
    membership:
      $ref: "#/components/schemas/Membership"
