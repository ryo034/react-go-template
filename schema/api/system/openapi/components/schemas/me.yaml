MultiFactor:
  type: object
  required:
    - factorId
    - phoneNumber
  properties:
    factorId:
      type: string
    phoneNumber:
      type: string

Me:
  type: object
  required:
    - emailVerified
    - member
  properties:
    emailVerified:
      type: boolean
      default: false
    multiFactor:
      $ref: "#/components/schemas/MultiFactor"
    member:
      $ref: "#/components/schemas/Member"
