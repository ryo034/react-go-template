JwtToken:
  type: object
  required:
    - token
  properties:
    token:
      type: string
      description: JWT token

AuthProvider:
  type: string
  description: Authentication provider
  enum:
    - email
    - google
