login:
  post:
    summary: Login
    description: Login
    operationId: login
    responses:
      "200":
        description: Login response
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/Me"
      "500":
        $ref: "#/components/responses/InternalServerError"
